const { drawTextFit } = require('./drawTextFit');
const { drawHeader } = require('./drawHeader');
const { drawBadge } = require('./drawBadge');
const { drawFormIndicators } = require('./drawFormIndicators');
const { drawTeamRow, drawTeamLogo } = require('./drawTeamRow');
const { drawTable, drawTableHeader, drawTableRow } = require('./drawTable');
const { paginateTable } = require('./paginateTable');

module.exports = {
    drawTextFit,
    drawHeader,
    drawBadge,
    drawFormIndicators,
    drawTeamRow,
    drawTeamLogo,
    drawTable,
    drawTableHeader,
    drawTableRow,
    paginateTable
};