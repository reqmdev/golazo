import type { Theme } from '../core/types';
/** Compact sports-app top bar — left-aligned hierarchy, no centered billboard. */
export declare function AppHeader(props: {
    x: number;
    y: number;
    width: number;
    height: number;
    title: string;
    subtitle?: string;
    badge?: string;
    meta?: string;
    theme: Theme;
}): import("../core/SvgBuilder").SvgNode;
//# sourceMappingURL=AppHeader.d.ts.map