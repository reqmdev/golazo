import type { Theme } from '../core/types';
/** Goal-share bar derived from actual score data — not fabricated stats. */
export declare function GoalShareBar(props: {
    x: number;
    y: number;
    width: number;
    homeGoals: number;
    awayGoals: number;
    homeLabel: string;
    awayLabel: string;
    theme: Theme;
}): import("../core/SvgBuilder").SvgNode;
//# sourceMappingURL=StatBar.d.ts.map