import { SvgChild } from '../core/SvgBuilder';
import type { Theme } from '../core/types';
/** Flat card shell — no decorative chrome. */
export declare function CardFrame(props: {
    width: number;
    height: number;
    theme: Theme;
    children: SvgChild | SvgChild[];
}): import("../core/SvgBuilder").SvgNode;
//# sourceMappingURL=CardFrame.d.ts.map